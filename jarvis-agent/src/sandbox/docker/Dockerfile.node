# Sandbox Node para ejecutar JS dentro de /workspace
FROM node:20-slim

WORKDIR /workspace

# (Opcional) utilidades básicas
RUN apt-get update && apt-get install -y --no-install-recommends \
    ca-certificates \
 && rm -rf /var/lib/apt/lists/*

CMD ["node", "--version"]